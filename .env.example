# DevOps Deploy Configuration
# Copy this file to .env and update with your actual credentials

# Server URL (required)
DEPLOY_SERVER_URL=https://your-deploy-server:8443

# Access Token (required)
DEPLOY_TOKEN=your-access-token

# Alternative environment variable names (also supported):
# DOD_SERVER_URL=https://your-server:8443
# DOD_ACCESS_TOKEN=your-token

# For test environments:
# TEST_SERVER_URL=https://your-test-server:8443
# TEST_ACCESS_TOKEN=your-test-token

# Optional: Use SimpleAuth token exchange (set to true if using personal access tokens)
# USE_SIMPLE_TOKEN_AUTH=true
